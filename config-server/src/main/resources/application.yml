spring:
  application:
    name: config-server
server:
  port: 7001
# git管理配置
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/chunchun2016/SpringCloud-Learning.git
          searchPaths: config-repo
          username: chunchun2016
          password: hcl2000